import { Request, Response } from "express";
// import sampleIds from "../db/seed/sampleIds.json"

const getSampleIds = (req: Request, res: Response) => {
  // res
  //   .status(200)
  //   .send({ success: true, msg: "List of sample ids", sample_ids: sampleIds });
};

export { getSampleIds };
